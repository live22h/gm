
<%= simple_form_for(@specialist) do |f| %>
  <div class="form-inputs">
    <%= f.input :order_id %>
    <%= f.input :user_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, 'Подать заявку на участие в проекте' %>
  </div>
<% end %>
